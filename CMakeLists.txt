cmake_minimum_required(VERSION 3.8)
project(worms_assets)

find_package(ament_cmake REQUIRED)

install(
  DIRECTORY
    meshes
    urdf
    xacro
  DESTINATION
    share/${PROJECT_NAME}
)

install(FILES README.md
  DESTINATION share/${PROJECT_NAME}
)

ament_package()